# NG AI SOC — public demo

The built console at [demo.sec-ops-lab.com](https://demo.sec-ops-lab.com). **Generated: do not edit here.** The source lives in the
product repository; this repository only holds what `npm run build:demo` produced, republished by
`deploy/demo/publish-pages.sh`.

Everything on the site is invented — the customer "ACME Corp", its people, addresses and events. No
real customer data, and no API: the page answers its own requests inside the browser.
